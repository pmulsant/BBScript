package com.code.generation.v1_3.elements.scope;

public class NormalCallableScope extends CallableScope {
    public NormalCallableScope(GlobalScope globalScope) {
        super(globalScope);
    }
}
